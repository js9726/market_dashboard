yfinance>=0.2.28
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
scipy>=1.9.0
requests>=2.28.0
beautifulsoup4>=4.11.0
# Pre-open CI holiday calendar check (refresh_premarket.yml).
# Used by scripts/check_trading_day.py to gate cron-driven jobs on NYSE
# trading days only — skips weekends + market holidays.
pandas_market_calendars>=4.4.0
# Gemini 2.5 Pro called via REST API — uses `requests` (already listed above)
# Morning brief multi-AI providers (install whichever keys you have)
openai>=1.70.0
anthropic>=0.49.0
# tradingview-screener wraps the unofficial scanner.tradingview.com endpoint.
# Optional — tv_screener_fetch.py uses urllib directly so this is informational.
# tradingview-screener>=2.0.0
