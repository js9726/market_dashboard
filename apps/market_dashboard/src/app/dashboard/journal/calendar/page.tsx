import JournalCalendarClient from "@/components/journal/JournalCalendarClient";

export const metadata = { title: "Calendar — Journal" };

export default function JournalCalendarPage() {
  return <JournalCalendarClient />;
}
