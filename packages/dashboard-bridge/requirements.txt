moomoo-api>=10.4.6408
requests>=2.31.0
tomli>=2.0.0; python_version < "3.11"
# Optional: Interactive Brokers adapter (ibkr_bridge.py).
# ib_insync requires Python 3.7+ and IB Gateway 10.19+ / TWS 10.19+.
# Install only if you use the IBKR adapter — MooMoo users don't need it.
ib_insync>=0.9.86
